<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TP1Agenda" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TP1Agenda" parent="Base.Theme.TP1Agenda" />

    <style name="CustomButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="shapeAppearanceOverlay">@drawable/shape</item>
    </style>

    <style name="FilterButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:strokeColor">@color/white</item>
        <item name="android:strokeWidth">2</item>
        <item name="android:background">@drawable/rounded_dark_bg</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>